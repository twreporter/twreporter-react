export default {
  image: {
    // top offset of the viewport you want to begin displaying your content
    loadingOffset: {
      placeholder: 600,  // extreme low resolution image to display at first
      image: 200          // real image to display
    }
  }
}
